<?xml version="1.0" encoding="UTF-8"?>
{% block content %}
<speedtests>
{% for st in speedtests %}
<speedtest>
	<status>{{ st.status }}</status>
	<download>{{ st.download }}</download>
	<upload>{{ st.upload }}</upload>
	<timestamp>{{ st.timestamp }}</timestamp>
</speedtest>
{% endfor %}
</speedtests>
{% endblock %}